# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'DynamicScheduler' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for DynamicScheduler
  pod 'CalendarKit'

  target 'DynamicSchedulerTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'DynamicSchedulerUITests' do
    # Pods for testing
  end

end
